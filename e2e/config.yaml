# Maestro E2E Test Configuration for Bibo Wine Cellar App

appId: com.winecellar.mobile # TODO: Update with actual bundle ID from Expo build

# Environment variables for test credentials
env:
  TEST_EMAIL: a
  TEST_PASSWORD: a
  
# Global settings
initFlow: ./flows/00-reset-state.yaml

# Screenshot directory
screenshotDirectory: ./screenshots

# Timeout settings
timeout: 10000
